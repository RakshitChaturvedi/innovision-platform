global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: alert_management
    static_configs:
      - targets:
          - alert_management:8010
    metrics_path: /metrics
